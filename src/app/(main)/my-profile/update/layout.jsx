export const metadata = {
  title: "Update profile",
  description: "Change your display name and profile photo URL for your SkillSphere account.",
};

const UpdateProfileRouteLayout = ({ children }) => children;

export default UpdateProfileRouteLayout;
