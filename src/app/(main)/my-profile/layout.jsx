export const metadata = {
  title: "My profile",
  description: "See your SkillSphere account name, email, and profile image in one place.",
};

const MyProfileRouteLayout = ({ children }) => children;

export default MyProfileRouteLayout;
